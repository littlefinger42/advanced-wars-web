import type { PropertySpriteDef } from "../models";

export const portPropertySprite: PropertySpriteDef = {
  motif: "port",
  tone: { base: 0x7dd3fc, dark: 0x075985, light: 0xe0f2fe, accent: 0xf0f9ff },
};
